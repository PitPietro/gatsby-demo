/**
 * Implement Gatsby's Browser APIs in this file.
 *
 * See: https://www.gatsbyjs.com/docs/browser-apis/
 * Custom CSS: https://www.gatsbyjs.com/docs/tutorial/part-two/
 */

import "./src/styles/global.css"
// require('./src/styles/global.css')

/**
 * Note: Both CommonJS (require) and ES Module (import) syntax work here.
 * If you’re not sure which to choose, import is usually a good default.
 * When working with files that are only run in a Node.js environment
 * however (like gatsby-node.js), require will need to be used.
 */

